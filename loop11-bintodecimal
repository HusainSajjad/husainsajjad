#include<stdio.h>
main()
{
int dec=0,bin,r,i=0;
printf("Enter a binary number ");
scanf("%d",&bin);
do{
r=bin%10;
dec+=r*pow(2,i);
bin/=10;
i+=1;
}while(bin!=0)
printf("the decimal equivalent is %d",dec);
}
