#include<stdio.h>
main()
{
int n,sum=0,i;
printf("Enter a no: ");
scanf("%d",&n);
for(i=1;i<n,i++)
{
if(n%i==0)
sum+=i;
}
if(sum==n)
printf("Its a perfect number");
else 
printf("Its not a perfect no");
}
