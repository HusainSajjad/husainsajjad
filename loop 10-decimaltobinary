#include<stdio.h>
#include<math.h>
main()
{
int dec,bin,i=0,r;
printf("Enter a decimal no: ");
scanf("%d",&dec);
do{
r=dec%2;
bin+=r*pow(10,i);
dec/=2;
i++;
}while(n!=0)
printf("The binary equivalent is %d",bin);
}
